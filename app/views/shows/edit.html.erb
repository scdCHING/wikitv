<html>

<h1>Edit Show</h1>
<hr>
<% if !@message.nil? %>
	 <% @message.each_pair do |key, value| %>
		ERROR: <%= "#{key}: #{value}" %> 
	<% end %>
<% end %>

<%= render partial: "shows/form", locals: { show: @show } %>
</html>
