<html>
	<head>
		<style>
			body {
	background-color: #ddd;
}				
		</style>
	</head>
	<body>
		<div class="jumbotron">
  <h1>wiki TV</h1>
  <p>This is a wiki site for TV shows.</p>
  <p><a <a href="shows/new" class="btn btn-primary btn-lg">Add Show</a></p>
</div>
	<table class="table table-striped table-hover ">
  <thead>
   
  </thead>
  <tbody>
	<tr>
		<td><b>Show Title</b></td>
		<td><b></b></td>
		<td><b></b></td>

	</tr>
<% @shows.each do |show| %>
	<tr>
	<td><%= link_to show.title, show_path(show.id) %></td>
	<td><%= link_to "Edit", edit_show_path(show.id) %></td>
	<td><%= link_to "Delete", show_path(show.id), method: :delete, data: { confirm: "Are you sure you want to delete?" } %></td>
	</tr>
<% end %>
	</tbody>
	</table>
	</body>
</html>
